[pytest]
markers =
    smoke: chama a API real da OpenAI (custa e não é 100% determinístico). Roda só com `pytest -m smoke` e OPENAI_API_KEY configurada — fora disso, fica de fora do `pytest -m "not smoke"` usado no CI.
